{
  "name": "nutrients-sync-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for My Nutrients sync",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "wrangler": "^4.28.1"
  }
}
